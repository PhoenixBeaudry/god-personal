# Validator Resources

Static files loaded by validator runtime code.

- `prompts.yml` contains synthetic task generation prompts.
- `test_axolotl.yml` is the local Axolotl evaluation config used by text evaluation.
